# ByteTrack configuration for the RGB-D social perception pipeline.
# The thresholds act on YOLO person detections; track_buffer is in frames.
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.25
track_buffer: 40
match_thresh: 0.80
fuse_score: true
